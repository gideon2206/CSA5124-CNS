#include <stdio.h>
int main() {
    char str[] = "Hello World";
    char *ptr = str;
    int i;
    printf("Original String : %s\n", ptr);
    for (i = 0; ptr[i] != '\0'; i++) {
        ptr[i] = ptr[i] & 127;
    }
    printf("After AND with 127 : %s\n", ptr);
    char str2[] = "Hello World";
    ptr = str2;
    for (i = 0; ptr[i] != '\0'; i++) {
        ptr[i] = ptr[i] ^ 127;
    }
    printf("After XOR with 127 : %s\n", ptr);
    return 0;
}

OUTPUT:
Original String : Hello World
After AND with 127 : Hello World
ter XOR with 127 : 7_(
